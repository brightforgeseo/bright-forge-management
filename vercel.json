{
  "buildCommand": "rm -rf .parcel-cache && npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/supabase/(.*)", "dest": "https://echo-ai.tailfdbc33.ts.net:10000/$1" },
    { "src": "/bridge/(.*)", "dest": "https://echo-ai.tailfdbc33.ts.net:8443/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
