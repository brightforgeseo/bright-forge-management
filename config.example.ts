// Configuration file for portal settings
// Paid browser-side AI keys are intentionally unsupported.
// Echo requests must go through the local Hermes bridge.

export const config = {
  // Add non-secret, browser-safe config here.
};
